# =============================================================================
# PODCAST EXPERTS BACKEND - ENVIRONMENT VARIABLES
# =============================================================================
# This file contains all environment variables needed for the project
# Copy this file to .env.local for local development
# Never commit this file to version control (it's already in .gitignore)

# =============================================================================
# DJANGO CORE SETTINGS
# =============================================================================

# Django Secret Key (REQUIRED - Generate a new one for production!)
SECRET_KEY=django-insecure-YOUR_SECRET_KEY_HERE

# Debug Mode (True for development, False for production)
DEBUG=True

# Allowed Hosts (comma-separated for production)
ALLOWED_HOSTS=localhost,127.0.0.1,.herokuapp.com

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================

# Database URL (SQLite for development, PostgreSQL for production)
# For local development, leave as default (SQLite)
# For production: DATABASE_URL=postgresql://user:password@host:port/dbname
DATABASE_URL=sqlite:///db.sqlite3

# =============================================================================
# CLOUDINARY STORAGE (for media files)
# =============================================================================

# Cloudinary Cloud Name (REQUIRED for media storage)
CLOUDINARY_CLOUD_NAME=YOUR_CLOUDINARY_CLOUD_NAME

# Cloudinary API Key (REQUIRED for media storage)
CLOUDINARY_API_KEY=YOUR_CLOUDINARY_API_KEY

# Cloudinary API Secret (REQUIRED for media storage)
CLOUDINARY_API_SECRET=YOUR_CLOUDINARY_API_SECRET

# =============================================================================
# EMAIL CONFIGURATION
# =============================================================================

# Gmail SMTP Settings (for email verification and notifications)
EMAIL_HOST_USER=YOUR_EMAIL@gmail.com
EMAIL_HOST_PASSWORD=YOUR_APP_PASSWORD

# Frontend URL for email verification links
FRONTEND_URL=http://localhost:3000

# =============================================================================
# CORS AND SECURITY SETTINGS
# =============================================================================

# CORS Allowed Origins (comma-separated)
CORS_ALLOWED_ORIGINS=http://localhost:3000,https://podcast-backend-4e5439705bd3.herokuapp.com

# CSRF Trusted Origins (comma-separated)
CSRF_TRUSTED_ORIGINS=http://localhost:3000,https://podcast-backend-4e5439705bd3.herokuapp.com

# =============================================================================
# JWT TOKEN SETTINGS
# =============================================================================

# JWT Access Token Lifetime (in days)
JWT_ACCESS_TOKEN_LIFETIME=1

# JWT Refresh Token Lifetime (in days)
JWT_REFRESH_TOKEN_LIFETIME=7

# =============================================================================
# PRODUCTION SETTINGS (set these to True in production)
# =============================================================================

# Security Settings (set to True in production)
CSRF_COOKIE_SECURE=False
SESSION_COOKIE_SECURE=False
SECURE_HSTS_SECONDS=0
SECURE_HSTS_INCLUDE_SUBDOMAINS=False
SECURE_HSTS_PRELOAD=False

# =============================================================================
# OPTIONAL SETTINGS
# =============================================================================

# Static Files Storage (set to whitenoise for production)
# STATICFILES_STORAGE=whitenoise.storage.StaticFilesStorage

# Content Security Policy (enable in production)
# CSP_ENABLED=True

# =============================================================================
# DEVELOPMENT NOTES
# =============================================================================
# 
# 1. For local development:
#    - Set DEBUG=True
#    - Use SQLite database (default)
#    - Set CORS and CSRF to allow localhost
#    - Set security headers to False
#
# 2. For production:
#    - Set DEBUG=False
#    - Use PostgreSQL database
#    - Set all security settings to True
#    - Configure Cloudinary credentials
#    - Set proper CORS and CSRF origins
#
# 3. Required variables for production:
#    - SECRET_KEY (generate new one)
#    - DATABASE_URL (PostgreSQL)
#    - CLOUDINARY_* (all three)
#    - EMAIL_* (if using email features)
#
# 4. Security checklist for production:
#    - Change SECRET_KEY
#    - Set DEBUG=False
#    - Enable HTTPS (CSRF_COOKIE_SECURE=True)
#    - Set proper ALLOWED_HOSTS
#    - Configure CORS properly
#    - Enable CSP if needed
#
# =============================================================================
